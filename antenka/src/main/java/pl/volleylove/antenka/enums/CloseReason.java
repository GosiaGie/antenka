package pl.volleylove.antenka.enums;

public enum CloseReason {
    //todo - trigger which closes events with date passed current date
    //AUTO_CLOSE = when event date passed current date
    CLOSED_BY_ORGANIZER, OUT_OF_PLAYER_SLOTS, AUTO_CLOSE
}
